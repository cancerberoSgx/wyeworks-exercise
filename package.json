{
  "name": "bob",
  "description": "Group artists albums in trello boards. API and CLI support",
  "version": "1.0.0",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "scripts": {
    "test": "npm run build && npx ava",
    "build": "npm run clean && npx tsc",
    "clean": "rm -rf dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^12.12.7",
    "ava": "^2.4.0",
    "ts-node": "^8.4.1",
    "typescript": "^3.7.2"
  },
  "dependencies": {
    "cross-fetch": "^3.0.4",
    "node-spotify-api": "^1.1.1"
  }
}
